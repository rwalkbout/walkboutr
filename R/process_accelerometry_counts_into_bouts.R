process_accelerometry_counts_into_bouts <- function(accelerometry_counts) {
  # TODO:
  #  - Define input schema and validate inputs match.
  #   - What are the column names
  #   - What are the data types of the columns
  #   - Are null values allowed in each column and how are they represented.
  #  - Define the output schema and write some validation
  #  - Write the processing code in the middle
  print('processing accelerometry counts')
}
